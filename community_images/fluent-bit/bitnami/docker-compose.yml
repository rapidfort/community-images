version: '2'

services:
  fluent-bit:
    image: ${FLUENT_BIT_IMAGE_REPOSITORY}:${FLUENT_BIT_IMAGE_TAG}
    cap_add:
      - SYS_PTRACE
    ports:
      - '8020:8020'

  prometheus:
    image: docker.io/rapidfort/prometheus
    cap_add:
      - SYS_PTRACE