#!/bin/bash

set -x
set -e

# add common commands here which should be present in all hardened images

sleep 1
cp --version
mkdir --version
chmod --version
ls --version
mv --version
rm --version
ln --version
rmdir --version
chgrp --version
chown --version
touch --version
cat --version
less --version
grep --version
sed --version
awk --version
