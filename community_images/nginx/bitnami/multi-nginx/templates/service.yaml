apiVersion: v1
kind: Service
metadata:
  name: {{ include "multi-nginx.fullname" . }}
  labels:
    {{- include "multi-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multi-nginx.selectorLabels" . | nindent 4 }}
