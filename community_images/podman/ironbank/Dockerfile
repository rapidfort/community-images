# Use a base image
FROM docker.io/library/alpine:latest

# Example command to show image building
RUN echo "Building my Docker image with Podman"

# Set the default command to run when the container starts
CMD ["echo", "Hello, Podman!"]
