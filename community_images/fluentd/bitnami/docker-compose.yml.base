version: '2'

services:
  fluentd:
    image: @IMAGE
    user: root
    cap_add:
      - SYS_PTRACE
    ports:
      - '24224:24224'
      - '5140:5140'
    volumes:
      - fluentd_data:/bitnami/fluentd
volumes:
  fluentd_data:
    driver: local
