apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "multi-nginx.fullname" . }}-test-connection"
  labels:
    {{- include "multi-nginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "multi-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
